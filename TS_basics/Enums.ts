//Enums are two types - number and string (values)
enum empNames{
    sara = 1,
    noha = 2,
    bora = 3,
    kela = 4
} //explicit values
//without values its is attached with index values

console.log(empNames.sara);
console.log(empNames[2]);